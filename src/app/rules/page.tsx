import ComingSoon from "@/components/ComingSoon/ComingSoon";
import {Metadata} from "next";

export const metadata: Metadata = {
    title: "Fresh Accounts - Правила сайта",
    description: "Правила сайта",
};

export default function Rules(){
    return <ComingSoon/>
}
